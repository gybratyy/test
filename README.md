```
# 🧩 Тестовое задание

## 🛠️ Технологический стек

**Язык:** TypeScript
**Библиотеки:** React, MobX, Leaflet, React-Leaflet, TailwindCSS, Google-Polyline, Axios
```

## 1️⃣ Основная задача

Создать React-компонент, который позволяет:

### 📅 Выбор временного интервала

* Пользователь выбирает **дату и время начала** и **дату и время окончания** периода.
* Интервал должен быть визуально разделён (например, два поля: “от” и “до”).

### 📤 Отправка данных на сервер

* После выбора интервала пользователь нажимает кнопку **«Получить отчёт»**.
* При нажатии выполняется **POST-запрос** на API:

  ```
  https://glot.kz/api/v2/report/{id}/
  ```

  где `id` для теста — **5257**.

### ⚙️ Параметры запроса

Тело запроса должно иметь вид:

```json
{
  "id": 5257,
  "type": "analyze",
  "format": "json",
  "is_group": false,
  "from": 1761937200,
  "to": 1762023600
}
```

> ⚠️ Для авторизации потребуется токен пользователя (будет предоставлен отдельно).

---

## 🧾 Отображение данных

API возвращает **JSON-объект** со структурой, аналогичной примеру в задании.
Необходимо отобразить полученные данные в виде **таблицы**:

* Поле `static` — общие данные отчёта.
* Поле `items` — несколько таблиц, например:

  * `"type": "trip"` — таблица с поездками, может содержать вложенные массивы (поездки внутри поездки).
  * `"type": "parking"` — таблица с парковками.
* Все вложенные данные должны быть корректно отображены (например, в виде подтаблиц или раскрывающихся строк).

Для демонстрации работы сделать простую HTML-страницу с этим компонентом.

---

## 2️⃣ Дополнительное задание (опционально)

### 🗺️ Работа с картой

* В некоторых элементах данных (например, у поездок) есть поле `route`.
* Это строка с координатами, закодированными в формате **Google Polyline**.
* С помощью библиотеки **google-polyline** декодировать её в массив координат.
* Используя **React-Leaflet**, отобразить карту.
* При клике на нужную поездку (строку таблицы) — нарисовать маршрут этой поездки на карте.

---

## 💡 Итог

В результате должно быть:

1. Компонент с выбором даты и времени (от/до).
2. Кнопка, отправляющая POST-запрос и отображающая ответ в виде таблицы.
3. (Дополнительно) Карта с визуализацией маршрута выбранной поездки.

---
